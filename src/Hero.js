import './Nav.css';

function Hero(props) {
  const heroBackground = {
    backGroundImage: './image/red.jpg',
    height: '200px',
  };
  return <header style={heroBackground}>hello</header>;
}

export default Hero;
